# Copy this file to backend/.env and fill in your real connection string.
# Never commit the real .env file.

# --- Option A: MongoDB Atlas (free cloud cluster) -----------------------
# 1. Create a free cluster at https://www.mongodb.com/cloud/atlas/register
# 2. Database Access -> add a user (username + password)
# 3. Network Access -> allow your IP (or 0.0.0.0/0 for local dev/testing)
# 4. Connect -> "Drivers" -> copy the connection string below
MONGODB_URI=mongodb+srv://madhumitha_db_user:madhubr918@bacterial-sim-cluster.opntlfk.mongodb.net/?appName=bacterial-sim-cluster
# --- Option B: local MongoDB ---------------------------------------------
# MONGODB_URI=mongodb://localhost:27017

# Database name (created automatically on first write, no manual setup needed)
MONGODB_DB_NAME=bacterial_sim
